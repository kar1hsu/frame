GROUP_NAME=frame
MYSQL_ROOT_PASSWORD=root
MYSQL_DATABASE=frame
REDIS_PASSWORD=
# 应用时区：注入到 server/worker/scheduler 的 TIMEZONE，决定应用 time.Local
# （日志 / JWT / 定时任务 / 数据库时间）。MySQL、Redis 不依赖它。
TZ=Asia/Shanghai
